
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("МассивЗначений") И ТипЗнч(Параметры.МассивЗначений) = Тип("Массив") Тогда
		
		Для Каждого ЭлементМассива Из Параметры.МассивЗначений Цикл
			НовСтр = ТаблицаЗначенийМассива.Добавить();
			НовСтр.ЗначениеМассива = ЭлементМассива;
		КонецЦикла;
		
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура ОК(Команда)
	Закрыть(ТаблицаЗначенийМассива);
КонецПроцедуры
